name: Lint Code

on:
  pull_request:
    branches:
      - main
      - development
  push:
    branches:
      - main
      - development
  workflow_dispatch:

jobs:
  lint:
    name: Lint & Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📚 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "any"
          channel: "stable"
          cache: true

      - name: 📦 Get dependencies
        run: flutter pub get

      - name: 🔧 Run code generation
        run: |
          dart run easy_localization:generate -S assets/translations -O lib/core/languages -f keys -o locale_keys.g.dart
          dart run build_runner build --delete-conflicting-outputs

      - name: 🔍 Run Flutter Analyzer
        run: flutter analyze --no-fatal-infos
